<br />
@*<div><button type="button" class="btn btn-primary" data-toggle="modal" id="myBtn" data-target=".b-example-modal-lg">Add </button></div>*@





<div class="card-header" style="background:  #194771; color: white;">
    <div class="row">
        <div class="col-md-8">
            <h3 style="color: gold;">Brand Name</h3>



        </div>
        <div class="col-md-4" style="position: relative;">
            <div class="btnAllRow">
                <button title="Add Brand"
                        style="margin: 2px;"
                        class="btn btn-success"
                        data-toggle="modal" id="myBtn" data-target=".b-example-modal-lg">
                    <i class="fa fa-plus-circle" aria-hidden="true"></i>&nbsp;Add Brand
                </button>
                @*<button title="Edit Room"
                            style="margin: 2px;"
                            data-toggle="modal"
                            data-target="#edit"
                            class="btn btn-warning"
                            (click)="updateOrder()">
                        <i class="fas fa-edit" aria-hidden="true"></i>&nbsp;Edit
                    </button>
                    <button title="Delete Room"
                            style="margin: 2px;"
                            data-toggle="modal"
                            data-target="#deleted"
                            class="btn btn-danger">
                        <i class="fas fa-trash-alt" aria-hidden="true"></i>&nbsp;Delete
                    </button>*@
            </div>
        </div>
    </div>
</div>

<hr />

<div class="modal fade b-example-modal-lg " tabindex="-1" role="dialog" data-backdrop="static" aria-labelledby="myLargeModalLabel exampleModalCenterTitle" aria-hidden="false" id="brandModal">

    <div class="modal-dialog modal-lg b-example-modal-lg ">
        <div class="modal-content">
            <div class="modal-header ">

                <h5 class="modal-title  text-center" style="margin: 0 auto;" id="exampleModalLabel"></h5>
                <p>  </p>
                <button type="button"
                        class="close"
                        data-dismiss="modal"
                        aria-label="Close">
                    <span>&times;</span>
                </button>
            </div>

            <div class="modal-body crudForm">
                <div class="col-md-12">
                    <div>
                        <div class="form-group">
                            <form>
                                <div class="row">
                                    <div class="form-group col-md-3 ">
                                        <label for="EmployeeId">Brand Name</label>
                                        <input type="text" class="form-control" id="BrandName" placeholder="Name" />
                                    </div>
                                    <div class="form-group col-md-3 ">
                                        <label for="EmployeeId">Status</label>
                                        <select id="bStatus" class="form-control" aria-describedby="basic-addon2">
                                            <option value="1">Yes</option>
                                            <option value="0">No</option>
                                        </select>

                                    </div>

                                </div>

                                <div class="modal-footer justify-content-center">

                                    <span><input type="button" class="btn btn-primary" id="btnBrandAdds" onclick="saveOrUpdate()" value="Save"></input></span>
                                    @*<span><input type="button" class="btn btn-primary" id="btnAdds" onclick="return cleardatda();" value="Clear"></input></span>*@
                                    <span><button type="button" class="btn btn-danger" data-dismiss="modal" id="btnBrandCencel" (click)="bsModalRef.hide();  onclick=" cleardatda()">Cancel</button></span>
                                </div>

                            </form>
                        </div>
                    </div>



                </div>

            </div>



        </div>
    </div>
</div>





<div class="table-responsive">
    <table id="datatablebrand" class="table table-striped table-bordered" style="width:100%"></table>
</div>


<script>
    var title = "Brand";

    document.getElementById("exampleModalLabel").innerHTML = title;

    $(document).ready(function () {
        getUnderList();
        showGrid();

    });


    var selectdata = "";
    function saveOrUpdatecencel() {
        //alert("dds")
        $.ajax({
            url: '/Item/Index',
            type: 'GET',
            //data: { StudentIdC: studentIdC },
            dataType: 'html',
            contentType: 'application/json',
            success: function (data) {
                $('#menu3').html(data);

                //Spiner.hide();
            },
            error: function (ex) {

            }
        });
    }

    function saveOrUpdate() {

        var btn = $('#btnBrandAdds').val()


        if (btn == "Save") {

            saveBrandName();


            //$.ajax({
            //    url: '/Brand/Index',
            //    type: 'GET',
            //    //data: { StudentIdC: studentIdC },
            //    dataType: 'html',
            //    contentType: 'application/json',
            //    success: function (data) {
            //        $('#menu6').html(data);
            //        //Spiner.hide();
            //    },
            //    error: function (ex) {
            //        //alert(ex);
            //        //Spiner.hide();
            //    }
            //});

        } else {

            UpdateBrand(selectdata.SERIAL);
        }
    }

    function saveBrandName() {
        //alert("che")
        var empObj = {
            BRAND_NAME: $('#BrandName').val(),
            B_STATUS: $('#bStatus').val(),

        };
        //alert(empObj)
        $.ajax({
            url: "/Brand/Create",
            data: JSON.stringify(empObj),
            type: "POST",
            contentType: "application/json;charset=utf-8",
            dataType: "json",
            success: function (result) {

                if (result == "Ok") {
                    $('#brandModal').modal('hide');
                    cleardatda();
                    showGrid();
                    swal("Save!", "Save success.", "success");
                } else {
                    swal("Error!", "Save Fail.", result);

                }
                ;

            }
        });

    }

    function UpdateBrand(sl) {
        //alert(sl)
        var empObj = {

            BRAND_NAME: $('#BrandName').val(),
            B_STATUS: $('#bStatus').val(),
            intBrandID: selectdata.intBrandID,


        };


        $.ajax({
            url: "/Brand/updateData",
            data: JSON.stringify(empObj),
            type: "POST",
            contentType: "application/json;charset=utf-8",
            dataType: "json",
            success: function (result) {
                //alert(result)
                //loadData();
                //$('#myModal').modal('hide');
                if (result == "Ok") {
                    $('#brandModal').modal('hide');
                    cleardatda();
                    showGrid();
                    swal("Update!", "Update success.", "success");
                    //alert("save data")
                    //$('.nav-tabs a[href="#tabLeaveApplicationList"]').tab('show');


                };
                //error: function (errormessage) {
                //    alert(errormessage.responseText);
                //}

            }
        });

    }


    function showGrid() {
        $('#datatablebrand').DataTable({
            "bDestroy": true,
            "processing": true,
            //"orderMulti": false, // for disable multiple column at once
            //"scrollX": true,

            "deferRender": true,
            "ajax": {
                "url": "/Brand/showGrid",
                "type": "POST",
                "datatype": "json",
                "dataSrc": function (response) {
                    // Settings.
                    response.draw = response.data.draw;
                    response.recordsTotal = response.recordsTotal;
                    response.recordsFiltered = response.recordsFiltered;
                    console.log('request.responseText', response.data);
                    return response.data;
                }
            },


            "order": [[0, 'asc']],
            "columns": [
                {
                    title: 'SL',
                    render: function (
                        data,
                        type,
                        row,
                       meta
                    ) {
                        return '<span>' + (meta.row + 1) + '</span>';
                    },
                },
                {

                    title: 'SERIAL',
                    data: 'intBrandID',
                    name: 'intBrandID',
                },

                    {

                        title: 'BRAND NAME',
                        data: 'BRAND_NAME',
                        name: 'BRAND_NAME',
                    },



                  {
                      title: 'Status',
                      data: 'B_STATUS',

                      render: function (data) {
                          return data == 1 ? "Active" : "Inactive"
                      }
                  },



                   {
                       render: function (data, type, row) {
                           return '<button class="btn btn-info fontsize  sales-sloat-ak">   <i class="fa fa-pencil"></i></button>' + "&nbsp" + '<button class="btn btn-danger fontsize  delete-sloat-ak"> <i class="fa fa-trash"></i></button>';
                       }
                   },

                     //{
                     //    render: function (data, type, row) {
                     //        return '<button class="btnPrintAllResult gotoResultEntry btn btn-success"><i class="fa fa-trash"></i> </button>';
                     //    }
                     //},



            ],


            "responsive": false,
            "select": true,
            "rowCallback": function (row, data) {


                $(row)
                 .bind('click')
                 .click(function () {

                     selectdata = data;
                     console.log('hello select data', selectdata);
                     //rerender();
                 });
                $(row).find(".sales-sloat-ak").click(function () {
                    selectdata = data;
                    //alert(data);
                    edit(selectdata);
                    //that.salesss(data);
                    //that.rerender();
                });
                $(row).find(".delete-sloat-ak").click(function () {
                    selectdata = data;
                    deletef(selectdata.intBrandID)
                    //that.salesss(data);
                    //that.rerender();
                });

            }

        });
    }
    function edit(selectdata) {
        //alert(selectdata.NAME)
        console.log('seleceted data', selectdata);
        $('#brandModal').modal('show');
        $('#BrandName').val(selectdata.BRAND_NAME);
        $('#bStatus').val(selectdata.B_STATUS);
        $('#intBrandID').val(selectdata.G_STATUS);
        $('#btnBrandAdds').val("Update");

    }

    function getUnderList() {

        $.ajax({
            url: '/StockGroup/getUnderList',
            type: 'GET',
            data: "{}",
            success: function (data) {
                console.log('hello delete dataddddddddd', data);
                var s = '<option value="-1">Please Select a Under Group</option>';
                for (var i = 0; i < data.length; i++) {
                    s += '<option value="' + data[i].NAME + '">' + data[i].NAME + '</option>';
                }
                $("#UnderG").html(s);
            },

            error: function (ex) {

            }
        });
    }

    function deletef(id) {
         

        $.ajax({
            url: "/Brand/DeleteB?id=" + id,
            //data: JSON.stringify(empObj),
            type: "POST",
            contentType: "application/json;charset=utf-8",
            dataType: "json",
            success: function (result) {
                

                if (result == "OK") {

                    cleardatda();
                    showGrid();
                    swal("Delete!", "Delete success.", "success");


                };


            }
        });


    }
    function cleardatdaddd() {
        this.modalForm.reset();
    }

    function cleardatda() {
        //alert("btn")

        //$("#datatable").dataTable().fnDestroy();
        //$('#StockGroupName').val("");
        //$('#UnderG').val("");
        //$('#gStatus').val("");
        $('#btnBrandAdds').val("Save");

    }

</script>
 

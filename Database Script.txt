CREATE DATABASE ASST0001

	CREATE TABLE INV_BRAND(
		SERIAL numeric(18, 0) IDENTITY(1,1) NOT NULL,
		BRAND_ID varchar(50) NOT NULL CONSTRAINT PK_INV_BRAND_ID PRIMARY KEY,
		BRAND_NAME varchar(60) NOT NULL,
		BRAND_STATUS	Numeric(1) default 0,
		INSERT_BY varchar(60) NULL,
		INSERT_DATE datetime DEFAULT GETDATE() NOT NULL,
		UPDATE_DATE datetime null 
		) 